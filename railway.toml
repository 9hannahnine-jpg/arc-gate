[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn arc_gate:app --host 0.0.0.0 --port $PORT --workers 1"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "bendex-sentry"
